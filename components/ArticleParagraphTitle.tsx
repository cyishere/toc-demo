import styled from 'styled-components'

const ArticleParagraphTitle = styled.h1`
  color: ${({ theme }) => theme.colors.primary};
  margin: 0;
  margin-top: 32px;
  line-height: 1.2;
  scroll-margin-top: 24px;
`

export default ArticleParagraphTitle
